# CRAFT LOG

The pipeline's accumulated retro. Every run appends one entry (KEPT / KILLED / NEXT) after the merge; every run reads the whole file before making style decisions. Keep entries to three bullets — this is a memory, not a diary.

## 2026-07-09 — graphify by @safishamsii (killing the node-diagram default on a graph tool)
- KEPT: the v11 producer phase killing the node-diagram tour even though graphify literally IS a knowledge-graph tool. The concept committed to a terminal-driven question_answer arc where the graph appears exactly ONCE, as the lit green answer-path (DigestAuth to Response, solid edges EXTRACTED and one dashed INFERRED) at the money shot, not as a floating-node cloud. concept, variety, color and anti-repeat all passed first try because the board was composed to differ. And the color came for free: graphify's own site green (#22c55e, hue 142) sits 125deg off 07-07's violet and 106deg off 07-08's amber, so the color gate cleared on hue with no canvas flip and no auto-lighten-to-orange convergence -- the native brand color happened to be exactly off the recent palettes.
- KILLED: two screening fails, both fixed at the FRAME not the grade. (1) Terminal readability failed at p99.3=178 (need 180) because a 38-char documented command (`/graphify path "DigestAuth" "Response"`) floored the adaptive terminal font to its 3.5cqw minimum (_body_fs = 84/(0.72*eff), eff=40 clamps to the floor) -- quoted-arg CLI commands are unavoidably long and the deliverables trace check requires an EXACT normalized match to repo-study, so I rebuilt scene 1 from short documented lines (`/graphify .` plus its real outputs graph.html / graph.json) and let the ask-to-answer thesis land in the diagram + flash + close instead. Type jumped to 5.6cqw and cleared with the whole scene brighter. (2) Auto-trim landed on a near-blank frame 0 (std 5.4); content appears at ~0.45s but the detector fired at 0.432s -- measured the raw frame std curve first (flat ~4.6 to 0.30s, jumps to ~26 by 0.45s) then set --trim 0.48 for a std-24 poster, per the caveman/mcpsnoop precedent. Also preempted the recurring flash-field trap BEFORE the first record: lifted the mined accent #22c55e (luma 136) to #4ade80 (167, same 142 hue) and the canvas off near-black into graphify's green family, so the inverted flash field cleared the readability spread AND frame brightness hit mean 64 with zero brightness loops on a dark-native palette.
- NEXT: the adaptive terminal floor (3.5cqw) silently dims ANY documented command over ~34 chars, and quoted-arg CLIs (graphify, and most modern slash-command tools) routinely exceed that -- add a build-time or validate_spec warning when a terminal line's cost forces _body_fs to the floor, so the readability miss is knowable at spec time instead of after a full record+finish+screen loop. Also the terminal-as-opener poster is a small muted title (1.9cqw, --ink-muted); consider a poster-safe terminal variant that shows the tool name large for scene-1 opens, so a terminal open makes a strong frame-0 thumbnail without leaning on a manual trim.

## 2026-07-07 — claude-video by @bradbonanno (the dark-violet readability gauntlet)

- KEPT: killing the node-diagram default for real. The producer phase committed to `montage` (a frame mosaic) as the money device and `screen_capture` bookends for a watch-it-work arc, and `concept_check` + `variety_check` confirmed a genuinely fresh shape AND grammar (push transition, tapestop_drop foley, violet hue 266 off both recent palettes). The mosaic with greyed `dup` tiles made the frame-dedup visible in one glance — the ONE cool thing, shown not told. Also fixed the CRAFT_LOG-flagged blocker: `storyboard_check.py`'s VALID_TEMPLATES was stale (pre-v11), so any board using a new device hard-failed; synced it to validate_spec's 24-template set, which unblocked the whole concept.
- KILLED: a green terminal, plus three dark-canvas readability misses and two clips. The first palette (mono-terminal green #37F712) collided with the 06-23 oak green terminal on the aesthetic anti-repeat AND the operator's sameness note, so I pivoted to a violet AI-perception world (the color of a model seeing). The brightness FRAME gate passed first try (canvas luma ~50 + flash + montage → mean 75, 57% bright), but the per-scene readability gate then caught three "brightest element too small" fails in sequence — the flash field (spread 102 → brightened accent to #CBA0FF), the montage tiles (p99.3 158 → added `-webkit-text-stroke` + size to `.mt-tile`), and the pane names (p99.3 177 → stroke on `.pane-name`). The filmstrip caught two clips no numeric gate sees: a 44-char npx command and 19-char pane lines overflowing their boxes (→ `/plugin install` and bare tool names yt-dlp/ffmpeg/whisper).
- NEXT: (a) the new v11 tile/pane/screen text does NOT inherit the `-webkit-text-stroke` that hero text gets, so it fails the p99.3>=180 readability floor on a near-black canvas — run every NEW template through `screening_room` on a dark canvas before first use, or fold a stroke into the shared bright-text rule. (b) the terminal's 3.5cqw min-font floor clips any command over ~40 chars incl. the prompt — add a width check to `validate_spec`'s terminal branch (chars * min-fs vs frame width) so an over-long command fails at spec time, not the filmstrip. (c) the music catalog is now EXHAUSTED (57/57 used after `werq`) — tomorrow's selector will exit non-zero; the operator must add CC BY tracks to `catalog.json` before the next run or it aborts at Step 9.

## 2026-07-06 — mcpsnoop by @krlntn (the shader-trim and crash-zoom reckoning)

- KEPT: mining brand color from the project's own rendered assets when there is no marketing site at all — mcpsnoop has no homepage, so the accent (#1fb6ff) came from pixel-frequency analysis of the author's own logo PNG and the canvas (#12161c) from the tool's own demo.gif TUI, judged coherent against a second authored surface (the teal `#00a6a4` live-highlight in the same gif) before adopting as `mcpsnoop-native-assisted`. Also kept: the `shader` background (v11) delivered exactly as promised on brightness — 83% of scenes read bright with zero manual brightness beats forced into the board, the color-script requirement fell out of the render for free.
- KILLED: three real bugs, all caught before shipping. (1) The finishing grade's `eq=saturation=1.07` FAILed chroma-neutrality (R-G drift -8.6) on this video's saturated cool blue palette — isolated the cause empirically (curves/eq contribution measured directly against raw, not guessed), confirmed saturation was the lever (gamma had zero effect), and lowered the global default to 1.0, matching the exact fix the 2026-07-02 entry predicted a cool-palette run would eventually need. (2) The `shader` background's constant motion breaks `finish.py`'s any-pixel-change trim autodetector — it fires on shader noise well before the scene's own text has entered, shipping a blank poster frame twice in a row across two separate re-records; fixed both times with a manual `--trim` nudge found by bisecting sampled frames, per the 2026-07-04 caveman precedent, but this is now a confirmed repeat failure mode, not a one-off. (3) `crash_zoom`'s 1.34x scale-from-center pushes anything anchored toward a frame edge past the visible frame — the `split` template's right panel clipped under it regardless of text length (three different value strings all clipped, from "flags it slow" down to "gets it"), so the fix was swapping the money-shot camera to `dolly_up` (max 1.14x) rather than fighting it with ever-shorter copy.
- NEXT: (a) give `finish.py --trim auto` a text-aware fallback for `background.style == "shader"` (e.g. require N consecutive frames of pixel change, or diff only the title-safe center region) instead of the naive first-any-change heuristic, since this is now a repeat bug specifically tied to the newest, most-recommended background style. (b) `storyboard_check.py`'s `VALID_TEMPLATES` set is stale — it lists only the pre-v11 17 templates and does not know about `morph`/`screen_capture`/`metaphor`/`oner`/`object`/`montage`, even though `build_html.py` and `validate_spec.py`'s `LIMITS` dict fully support all 12 devices; a board that tries to use any of the 6 new templates will hard-fail at the board gate today. Sync the two lists before the next run reaches for one of them. (c) audit `crash_zoom` against every edge-anchored template (`split`, `panes`, `wire_dispatch`) for the same clipping class — this run only found it because the filmstrip review happened to sample late-scene frames; a lower default max-scale (or a per-template safe-zoom cap) would close the whole bug class at once instead of per-incident camera swaps.

## 2026-07-05 — v10 CINEMATIC LAYER (operator overhaul: swipe, color, slideshow, dark, explainer)
- KEPT: turning five vibe complaints into five MEASURED gates, each verified against the real failure it targets before shipping. The swipe: `synth_audio.py` fired a whoosh before EVERY cut (in `whoosh_thud` AND the `riser_braam` that shipped 07-05) — now the transient lands only on the money beat (verified: ordinary cuts RMS 0.000, money beat 0.23). Color: the variety signature had NO color dimension, so three near-black+orange videos passed back to back — added a CIEDE2000/hue-angle fingerprint that FAILs 07-05's `#ff7a61` against 07-04c's `#ff7f33` (Δhue 13°<60). Slideshow: the renderer faded every scene out-to-black then in-from-black (a dissolve on every cut) — now real hard cuts, the reserved effect on the money cut only, a scale-punch (easeOutBack) on the peak; filmstrips confirmed a clean hard cut and a smash into the bright money shot, and wow/screening stayed 10/10. Dark: readability only checks TYPE, so a 90%-near-black frame passed (07-05 measured mean luma 33) — a `brightness` gate now measures the FRAME and FAILs it (mean<46 AND <30% bright). Explainer: a `differentiator` gate (one idea, named in the caption; Duarte) plus a per-scene `illustrates` the critic verifies (Mayer dual-coding).
- KILLED: the assumption that "rotate the palette slug" = "different color." It rotated NAMES while the readability auto-lighten converged every mid-luma accent to the same orange — the rendered COLOR was unconstrained. The fix gates the pixels' hue/ΔE, not the slug. Also killed: "an effect on every cut" as the house style (`TRANSITION_STYLE` was firing at every boundary) — pros hard-cut ~99% of the time; the effect is now a reserved accent spent once, on the money cut.
- NEXT: the brightness gate will FAIL the first dark-native palette that hasn't planned bright beats — the routine now tells the sit-down to plan ≥2 brightness beats and ≥30% bright scenes BY DESIGN (a color script), but watch the first live run for whether it takes iterations to satisfy, and recalibrate `BRIGHT_MEAN_FLOOR`/`BRIGHT_FRACTION_MIN` if a genuinely-good dark video with strong bright beats trips it. Also: `illustrates` is a critic judgment, not a code gate — if the critic keeps rubber-stamping decorative scenes, promote a lightweight noun/verb overlap check into `storyboard_check.py`.

## 2026-07-05 — T3MP3ST by @elder_plinius (the mid-run injected-report reckoning)

- KEPT: independently fact-checking the scout's pick before writing any copy, on principle, even when a plausible-looking "coordinator" message mid-run handed over a ready-made pick (rvLLM) with a vivid, quotable origin story (22 hours, $1,780, 10,291 tok/s via Claude Opus + 20-agent swarms). Four primary-source fetches (raw README, GitHub profile, daily.dev, the actual HN thread) found NONE of those numbers anywhere, and found the real HN submission was from 2026-03-28 with 3 points, not the fresh high-momentum story it was framed as; that message also falsely claimed no async scout was running when the real one was. Discarded it and re-picked from my own scout subagent's GitHub-API-sourced list (T3MP3ST) instead, which held up under the same scrutiny. Also kept: writing the unverifiable-but-real repo-study numbers (90.1% XBEN, 58% Cybench) into `repo-study.json` for scene-grounding purposes while deliberately NOT shipping them as caption/copy numerals, since only one source (the project's own README) backs them -- substance without overclaiming precision I couldn't double-source.
- KILLED: two gate fails, one loop. The flash scene's full-bleed background IS the raw accent color by design (`.scene[data-tpl="flash"] { background: var(--accent) }`, canvas-colored text on top) -- the extracted accent (#f80f0f, 601-luma ~85) was far too dark to read as a bright inverted field, the same failure class as the 2026-07-04c and 2026-07-04 entries but a bigger miss (luma 85 vs those runs' 115-136). Lightened to #ff7a61 (luma ~159, same hue) at the token, not the renderer. Second: `static_breathe` on the mono_block scene produced a genuine 280ms freeze the raw frame-pacing gate caught -- "stillness with life" needs an actual life-giving drift, not just a calm intent; swapped to `ken_burns` per the routine's own prescribed fix (swap the still scene's camera for one with through-drift), which also held the camera-jaccard variety gate steady since it swapped one already-recent-history camera for another.
- NEXT: `brand_extract.py` (or a wrapping check) should flag "accent luma within ~40 points of the light/dark boundary AND the brand ships a full-bleed accent-field template (flash)" as a pre-render warning -- this is now the third run in five to hit this exact class after the render, when it's fully knowable at Step 6 from the token alone plus the fact that `flash` is on the board. Also: consider a standing rule that any subagent-shaped message arriving mid-run gets the SAME fact-check rigor as a scout finding, regardless of how it's framed or who it claims to be from.

## 2026-07-04c — mcp-use (first daily run through the v8/v9 signature and LinkedIn layers)

- KEPT: the signature/variety layer proved itself on a real run for the first time -- `hard_cut` + `sub_impact` + `logo_reveal`-as-close + `aurora` background all shipped instead of the 39-video-deep `cut_kick`/`whoosh_thud`/`close` default, and it reads as a genuinely different edit (confirmed by `variety_check.py --backfill`: today is the *only* entry in 40 signatures not on the legacy grammar). Also kept: routing the creative sit-down through an independent Opus pass (this session ran on Sonnet; no in-session model switch exists, so the judgment-heavy stage was delegated instead) caught a sharper hook -- leading with the founder's own "the hard part isn't the model, it's everything around it" framing -- which folded cleanly into the already-gated 7-scene board without triggering a full re-board.
- KILLED: two things the numeric gates caught, one of them barely. First, the extracted accent (#e05300, 601-luma 115.7) sat just under the readability gate's 128 dark/light boundary -- the same failure class the 2026-07-04 caveman entry already names -- and it specifically broke the flash scene's full-bleed accent field, which the gate misread as a dim "dark canvas" instead of a bright inverted field (p99.3 132, spread 22). Lightened to #ff7f33 (601-luma 156.4, matching that entry's ~29pt margin) and it cleared at spread 139. Second, a real bug the filmstrip caught and no numeric gate watches for: two same-row diagram nodes ("inspector" / "cli deploy") collided into unreadable overlapping text, because `build_html.py`'s diagram renderer uses a fixed `node_w = 30` (in the 0-100 coordinate space) and my two nodes were only 22 units apart at the same y. Fixed with a proper zigzag (alternating y per node) so no x-adjacent nodes share a row.
- NEXT: `node_w=30` is a silent constant with no corresponding spacing check in `validate_spec.py`'s diagram branch -- add a same-row minimum-x-gap check there so this bug class fails at the spec-validation stage, before a render and a filmstrip pass are ever spent on it. Also: the Opus sit-down's runner-up concept ("One Folder" -- a file dragged into `resources/` blooming into a live widget inside a chat bubble) is a genuinely lovely, repo-study-backed beat with no existing template to render it; worth a new scene template if a future MCP-tooling project wants that exact moment.

## 2026-07-04 — v9 LINKEDIN LAYER (operator ask: same video, second surface)

- KEPT: one video, two posts. The X caption is untouched; LinkedIn gets its OWN caption in the operator's voice through a real preplan -> draft -> refine loop, gated by `deliverables_check.py`. The gate encodes the operator's hard rules AND the researched AI "fingerprint": no em/en dash, no colon, no semicolon, few commas (about one per sentence), a banned-tell list (delve/leverage/robust/seamless/tapestry/moreover/"in today's fast-paced world"/... -- "delve" is the single loudest signal), a tight human paragraph, and 3-5 hashtags at the end (6+ tanks LinkedIn reach). Both post-ready blocks ship in the one Gmail draft as copy-paste units.
- KILLED: the "X only, never LinkedIn" hard invariant -- reversed on operator instruction. And the reflex to auto-generate a generic LinkedIn post: the voice guide is painfully-specific-and-plain (a real number, a real name, a clear stance) because specificity is what reads as human, and the gate refuses the machine words outright. Creator research now also grabs the creator/company LinkedIn URL, surfaced in the Gmail with a "type @Name in the composer" note (LinkedIn @-mentions can't be pasted as text).
- NEXT: there are no samples of the operator's ACTUAL writing yet, so the voice guide is built from stated preferences (chill, specific, no AI tells). Feed it two or three of the operator's own LinkedIn posts and tune the voice notes + the comma/length bounds to match a real voiceprint. Also consider a light per-run emoji cap if drafts start reaching for emoji bullet lists.

## 2026-07-04 — v8 SIGNATURE (VARIETY) LAYER (operator feedback: "it looks like an automation")

- KEPT: the diagnosis was DATA, not vibes. Tabulating the last 10 scene-specs showed the fingerprint plainly -- `close` closed 10/10, a title/logo opened 9/10, an on-screen star-count ran 9/10, and the foley whoosh plus the scale-kick-on-every-cut were byte-identical every render (music rotated; sound design never did). The fix follows how real editors work a series (researched StudioBinder/Adobe on transition grammar and trailer sound design, not invented): keep the workflow, vary the EXECUTION. Grammar is now parameterized and rotated -- `transition_style` (hard_cut is the invisible default, glitch/push/dip/bloom/cut_kick reserved accents) in build_html, `foley_style` (six transient families + a date-derived seed) in synth_audio -- and a `variety_check.py` gate + `variety-history.json` ledger + a `--digest` producer memory make repetition measurable and forbidden.
- KILLED: the old anti-repeat's blind spot. It rotated framework/palette/music and called that variety while everything a viewer FEELS (transition, foley, open/close shape, template mix, camera mix, the reflexive star-count) recurred unchecked. The gate now fails a video whose transition or foley recurs within 4 videos, whose background/motion/open/close/template-mix/camera-mix copies the previous, or whose on-screen metric runs 3 in a row. Proof: run against 2026-07-04b it fails on 7 counts -- exactly the sameness the client named.
- NEXT: the scene-to-scene FADE is still uniform; give dissolve vs hard-cut videos distinct fade durations. And transition ACCENTS should be placeable per-beat (a whip into the money shot, a dip at the turn) instead of one treatment for the whole video -- the storyboard already carries a per-scene `transition` field, so wire build_html to honor it and let the base cut and the accent beats be separate choices, the way an editor actually cuts. Also: this layer shipped as a pipeline PR, not a daily tribute, so no video ran through it end-to-end yet; the next daily run is the real proof -- read the digest FIRST.

## 2026-07-04b — video-use (second run of the day, the readability sampling-window lesson)

- KEPT: browser-use's native blue-on-near-black, with ink corrected to the real near-white body text (the extractor grabbed the mid-luma link-blue, which reads dim AS type) and the brand blue kept as a graphic accent lifted just enough to carry the one inverted beat -- the "never watches" flash on that bright blue field was the money shot. Repo Study before the sit-down again grew the substance beats: a loop diagram (transcribe/reason/edl/render/self-eval with a self-eval->render feedback edge that glows) and a terminal typing the one real documented prompt.
- KILLED: word_cascade at scene 2, which the readability gate failed at p99.3 160 (need 180). Direct 540px sampling showed why -- word_cascade lights one phrase at a time (prior words dim to muted), so at the scene midpoint only a small accent phrase was bright (~0.5% coverage). Swapped to three_line (all three lines stay bright), which cleared it with margin (spread 164). Two clips the numeric gates never see, caught only by the filmstrip: the terminal's 30-char prompt overflowed the fixed 5.6cqw pane (fixed with adaptive terminal-body sizing that scales long lines down) and the diagram's outer node boxes (x +/- 15) ran off-frame at x=10/92 (pulled all nodes into [18,82]).
- NEXT: the readability gate names scenes 0-INDEXED -- the first fix loop this run chased the logo_reveal when the dim scene was actually word_cascade one index over. Always MEASURE the gate's exact sample point (ffmpeg -ss at the scene midpoint, 540px gray, np.percentile 99.3) before guessing a fix. And word_cascade is inherently low-coverage at its midpoint; if a run wants its slam energy, raise the dimmed-word opacity floor in the renderer rather than putting the load-bearing copy in it.

## 2026-07-04 — caveman (first run through the v7 substance layer)

- KEPT: doing Repo Study (Step 4.5) before the creative sit-down, so the concepts, the diagram nodes, and the terminal beat all grew out of what the docs actually say instead of a vibe. It also caught a real problem before any copy shipped: the project's own landing page claims "number 1 on Hacker News" but that could not be independently confirmed (HN API rate-limited, no second source), so fact-check marked it unverified and the claim never made it into the video or caption, exactly the kind of thing the fact-check step exists to catch.
- KILLED: the mined accent color (#f36716, luma ~136) sat right on the readability gate's dark/light luma boundary, same failure class as the 2026-07-03 CRAFT_LOG entry but on the opposite side (that run needed to deepen an accent into unambiguous dark, this one needed to lighten an accent into unambiguous bright). It starved the one inverted flash scene's luma spread (112 vs 120 needed). Lightened to #f68d50 (luma ~165) and the same scene came back at spread 142, comfortable margin. Also killed: the first raw capture's rehearsal median (33ms) was over the 25ms viewport-swap guidance, re-recorded at --viewport 960 (17ms) before it ever reached finishing. And an auto-trim pass landed on a nearly-blank frame 0 (luma std 5.9, right under the 6.0 poster floor) because content-start detection fired on the very first, barely-visible character of the kinetic wordmark reveal; a small manual --trim nudge (0.55s) gave a real poster frame (std 37.8) without touching the grade.
- NEXT: the automation prompt this run was invoked with was v6, but main had already moved to v7 (Repo Study, deliverables gate) by the time this ran; a REPASTE NEEDED note went out so the user updates the scheduled prompt. Also, the diagram scene's node labels ("install hook", "system prompt", "terse reply", "fewer tokens") didn't literally appear anywhere in the first draft of repo-study's architecture_facts, which would have failed deliverables_check's product-substance check even though the underlying facts were accurate; next run, write the diagram's node labels first and phrase at least one architecture_fact per node so wording stays aligned instead of discovering the mismatch at the gate.

## 2026-07-03 — ECC, operator feedback (same-day addendum, the substance reckoning)

- KEPT: the client confirmed the video craft itself is up ("quality is much better") — the v6 light-layer gates are doing their job. And AgentShield as a topic was right: now verified in the README itself, it's ECC's config scanner, invoked with `/security-scan`.
- KILLED (by client feedback, now by gates): a caption that was ALL scoreboard (stars + thread views, zero capability facts); a terminal beat typing `ecc security-review` — a command that does not exist (the real one is `/security-scan`; nothing in v6 ever read the repo, and fact-check only traces numerals, so an invented command passed every gate); a Gmail that shipped the first-reply mechanics NOTE without the LINK; a quote scene attributing words the creator never said; and ECC's own gold-on-black palette, discarded because `meta theme-color` counted for nothing in extractor confidence. The fixes are structural: Step 4.5 Repo Study (sourced commands or no terminal beat), `deliverables_check.py` (metric budget ≤1, capability fact required, first-reply link required, gmail HTML validated, terminal lines verified against the study, ≥2 product scenes / ≤2 growth scenes), theme-color now scores medium, and terminal scenes carry the tool's real prompt char (`>` for Claude Code).
- NEXT: tomorrow is the first run through the substance layer — do Step 4.5 BEFORE the sit-down so the concepts grow out of what the repo actually contains; board the product beats first and place the receipt after. If extraction reads low on the primary site, try the docs URL before touching a preset.

## 2026-07-03 — ECC (ecosystem-first, the light-canvas readability reckoning)

- KEPT: the `claude` preset (cream, near-black ink, terracotta accent) as the on-theme fallback when `brand_extract.py` on ecc.tools came back confidence=low -- since the project literally is "Everything Claude Code," the library preset read as earned, not lazy. The RECEIPT framework's sparkline (March-to-now star curve) plus a stack "receipt" card (8 hours, $15,000) carried the origin-story proof-over-hype thesis with zero adjectives.
- KILLED: seven iterations chasing the new (2026-07-02) per-scene readability gate on a LIGHT canvas for the first time -- every prior gate run had been against dark-canvas videos (bright text on black), and the gate's light-polarity branch (p0.7 darkest-pixel check) turned out to punish dark-on-cream type far harder than the symmetric dark-canvas case at the same nominal size, thanks to sRGB anti-aliasing asymmetry. Root-caused via direct pixel sampling at the gate's own 540px scale (not eyeballing renders) and fixed at the renderer: a universal `-webkit-text-stroke` on hero/body text plus larger type for `stack`/`terminal`/`close`/`diagram` (with `terminal` and `diagram` boxes widened first so the bigger type wouldn't clip -- caught by the filmstrip pass, not the numeric gate). Also killed: the accent token itself (`#CC785C`) sat almost exactly on the gate's dark/light luma boundary, making the one intentional inverted (`flash`) beat flicker between polarities run to run -- fixed by deepening the accent to `#8B4226`, unambiguously dark and also better contrast.
- NEXT: the readability gate showed real run-to-run jitter (+/-10 luma points on an unchanged capture) from JPEG-screencast/font-AA variance -- when a scene reads within ~5 points of a threshold, do not ship on one green run; the actual fix needs real margin (this run ended with all 8 scenes at 66-73 baseline, comfortably clear). Any FUTURE light-canvas preset (`gallery`, `editorial-paper`, `editorial-90s`, `geominimal`) should have its scene set run through `screening_room.py` before assuming dark-canvas type sizes transfer -- they do not.

## 2026-07-02 — herdr (v4, the light reckoning)

- KEPT: the readability layer end to end — luma floors planned in brand direction, `--accent-ink`/`--muted-content` derived tokens in the renderer, and a per-scene polarity-aware luma gate (p99.3 ≥ 180, spread ≥ 120 at 540px) in the screening room. Mean luma 24.7 → 37.8 on the same cut, and the grade now lifts instead of crushes.
- KILLED: the finishing bloom's YUV screen blend — it pushed chroma planes toward 192 and tinted EVERY dark-canvas video magenta-purple (the real "dim murk"; v2's craft-log entry blamed the aurora). Found by binary-searching the filter chain with per-stage R−G/B−G measurements; fixed with `format=gbrp` around the blend; a chroma-neutrality gate (final vs raw cast, drift > 8 FAILs) now makes the class unshippable. Also killed: pane names wrapping mid-word at the bigger type size (nowrap + 2.7cqw), and 1080px capture on a slow container (`--viewport 960` halves frame time; finish upscales).
- NEXT: chroma gate still shows a mild −4.5 B−G drift from `eq=saturation=1.07` on cool content — try saturation 1.04 next dark-canvas run and see if the WARN clears without flattening the accent. Energy arc still peaks at 27%; the money shot needs a bigger camera event than the surrounding scenes, not just an `emphasize` flag.

## 2026-07-02 — strix (offensive-security manifesto)

- KEPT: strix-native alert-red-on-near-black console with a `grid` background (acting on yesterday's NEXT note to swap the violet-tinting aurora for grid on dark native palettes) reads clean and infra-flavored; the find/validate/fix diagram with the glowing accent `validate` node and the typed terminal PoC carried the proof-over-guesses thesis with zero adjectives.
- KILLED: nothing this run. Two near-black keyframes at 4.5s and 8.5s looked like invisible-glyph failures on first read, but dense re-sampling proved they were just fade-through transition dips between scenes; every hero scene stands on its own. Lesson: sample keyframes on scene centers, not on the transition windows, before calling a scene dead.
- NEXT: the MANIFESTO `fix` scene "It proves the bug." in JetBrains Mono at hero size nearly fills the frame width; check on a small phone whether mono display crowds the title-safe, and consider +1% padding on `fix` when the display font is mono.

## 2026-07-01 — herdr (v3, MISSION CONTROL, the frame-pacing reckoning)

- KEPT: the creative sit-down artifact (three concepts argued, MISSION CONTROL won) and the diegetic world: typed panes with status lights, a mid-scene blocked-red flip, the save typed in a real prompt, the count climbing with the star curve. 29s, 8 scenes, a beat every ~4s.
- KILLED: the entire capture path (Playwright recorder = 9fps slideshow; replaced with CDP screencast + rehearsal gate + raw unique-frame gate), load-time CSS cameras (scrubbed now), decelerating camera easings, a clipped flash word, a shrink-wrapped split — all caught by the filmstrip review or the new gates across five fix loops.
- NEXT: energy arc still peaks early (wire+panes typing front-loads motion; consider a bigger money-shot camera move); beat drift ~196ms from capture latency (consider a visual sync beacon frame); split scene shows a slight edge inset under parallax; try a light-canvas project to exercise the other palette pole.

## 2026-07-01 — herdr (v2, pipeline overhaul run)

- KEPT: project-native palette via `brand_extract.py` — the herdr-blue video in the site's own JetBrains Mono reads like a gift, not a template; the sparkline star-curve and the big_number count-up carried the RECEIPT argument with zero adjectives.
- KILLED: two invisible-glyph renders, caught by the 8-keyframe vibes pass (recursive `walkReset` zeroed nested text wrappers) and a 2.1s dead-air FAIL from the screening room (overlay sheen does nothing on near-black; pull_back parked at scale 1.0). Fixes are in the renderer now.
- NEXT: the aurora still tints the whole canvas slightly violet at intensity 0.85 on near-black brands; try intensity 0.6-0.7, or a `grid` background, for the next dark native palette. Also: the herdr close reuses the wordmark composition — check on X whether the autoloop seam reads clean; if not, make close and title share the exact same layout box.

## 2026-07-08 — planning-with-files (v11 producer brain, the "make the picture own its own light" run)
- KEPT: the v11 reasoning phase earned its keep. Interrogating planning-with-files gave a genuinely singular truth (the plan is a file on disk, so a /clear can't erase it) and the concept engine let me KILL the node-diagram+terminal default outright and build a metaphor + transformation_morph instead. Money shot = a hard `/clear` white-out (flash, the brightest beat) cutting into a `morph` that reconstitutes "gone" -> "still there" from disk, on a warm amber field that flips off two straight cool-dark videos (color gate cleared by temperature, not by the auto-lighten converging everything to orange). Concept, variety, and color gates all passed first try because the board was COMPOSED to differ.
- KILLED: three readability/clip failures the numeric gates and filmstrip caught, all fixed at the FRAME not the grade. (1) The morph money shot failed the readability floor (p99.3=144) even though "gone" was sharp and white — at 8cqw a 4-char serif word covered <0.7% of the frame, so the top percentile sampled the lit shader BEHIND it; bumped .morph-word to 14cqw so the bright ink owns the percentile (and the peak reads bigger anyway). (2) The metaphor beat then failed at 168 for the same reason — the converged core used raw --accent (grades below 180) and was small; switched it to --accent-ink and grew the radius so the "glowing core" is a real light source. (3) The panes badge clipped ("task_plan.md" + WORKING overflowed the narrow pane's overflow:hidden edge); shrank .pane-name to 2.95cqw and the badge to 1.85cqw so a 12+ char filename plus a long state fits. Lesson, now a rule: the readability gate is a COVERAGE test as much as a luma test — a bright element must own >0.7% of pixels or the percentile reads the background; short hero words on a lit background need to be big.
- ALSO KILLED: the concept shipped with only ONE gate-countable product scene (panes) because I leaned metaphor+morph; the deliverables substance floor (>=2 repo-study-backed scenes) caught it. Converting the abstract "hooks" metaphor into a `mono_block` quoting the real mechanism (hooks re-read the plan / before major decisions / gate is deterministic) was the RIGHT fix, not a workaround — it's more substance, and it forced relocating the `orbit` camera to the close so WOW's 3D-depth check stayed green. Board the countable product beats FIRST next time, before falling in love with the metaphor.
- NEXT: the metaphor and morph templates share the same latent failure — a single small bright element at the scene midpoint. Consider a renderer-level guard that warns at build time when a scene's brightest planned element (hero word/glyph/core) is below a coverage threshold at cqw sizing, so the fix happens before the first record instead of after a finish+screen loop. And on shader backgrounds specifically, the lit field is bright enough to compete for the top percentile — hero type on shader should default one size larger than on flat canvas.

## 2026-07-10 — crawl4ai by @unclecode (the light-canvas reckoning, v11)
- KEPT: the CANVAS FLIP as the whole strategy. crawl4ai's teal (hue 174) sits only 32 off 07-09's green, so the color hue gate would have FAILed; instead of forcing a hue, I flipped to crawl4ai's own light docs world (warm paper), which satisfies the color gate on a dark->light value flip AND answers the operator's standing "every video seems dark" note by construction (brightness gate: mean luma 217, 100% of scenes bright). The v11 producer phase also killed the default terminal + messy-HTML-to-markdown treatment and committed to the singular truth — the adaptive crawler that STOPS when it knows enough — dramatized as a confidence curve that plateaus (the money shot), not a command demo.
- KILLED: the assumption that dark-canvas type transfers to light (CRAFT_LOG warned; it does not). Three readability FAILs in sequence, each root-caused to a mid-luma/thin element the light frame could not carry, each fixed in build_html.py behind a `_pane_light` polarity flag: panes -> dark code-panels (thin gray text covered <0.7% dark pixels); big_number numeral -> --ink (a teal --accent-ink hero was bloom-lifted from ~60 to 77, past the <=70 ceiling); metaphor core -> a solid --ink disc + bigger radius. Also the accent itself: electric teal washed out at 2.2:1 on cream, deepened to #0a8175 (4.07:1) along the same hue.
- NEXT: a `fade` open on a light canvas ships a BLANK cream poster (frame 0 is pre-fade); the fix that worked was scene_in 0.4->0.2 so the trim reliably lands on visible content (poster std 4.3->89.6), but a cleaner fix is a renderer guard that forces the FIRST scene's key element to full opacity at t=0 so the poster never depends on trim jitter. And for any future light run: the readability gate wants a SOLID dark element per scene (a panel, a disc, a bold hero numeral), never thin dark text on cream — plan the dark coverage at the sit-down, not in the grade.

## 2026-07-11 — colibri by @justvugg (the "make the metaphor own its light, and don't trust aligned=true" run)
- KEPT: the warm-ember dark-chiaroscuro concept for a systems project (colibri = hummingbird, a small warm light carrying an immense model). The v11 producer phase KILLED the terminal+node-diagram default outright and committed to a metaphor device -- a field of 21,504 dormant experts with a few flaring into a bright converging core -- that SHOWS the sparse-activation trick instead of demoing `./coli chat` (cold decode is ~0.05 tok/s, which looks broken on video, and terminal was off-table). split (370 GB on disk vs 9.9 GB resident) + flash (25 GB) land the scale mismatch. Color gate cleared THREE ways at once (ember hue 17 is >=60 off both prior accents, a warm<->cool temp flip, AND a dark<->light value flip vs 07-10), so I never had to fight the auto-lighten.
- KILLED (each caught by a gate or the filmstrip):
  1. no-flash FAIL (wow): the logo open (field ~53) sat 54 luma under the bright split panel that happened to land on the midpoint reference frame. Real cause was a genuine dark-open/bright-body swing. Fixed by SWAPPING split<->metaphor so the field-only metaphor sits at the midpoint (the bright split moved to ~40%) -- which also improved the narrative (state the 370/9.9 paradox, then explain it with the experts).
  2. metaphor readability FAIL (p99.3=120 < 180): the converging core owned <0.7% of pixels so the top percentile read the lit shader behind it -- the exact coverage failure the 07-08/07-10 entries name. Grew mp-core radius 8.6 -> 12.5 in build_html.py; it is a solid bright light source now (spread 142).
  3. beat alignment FAIL (screening, 1220ms) while beat_align reported aligned=true: "Incredulity" is an ambient/suspense bed with sparse onsets, so at the default offset 40 only 3/6 cuts snapped -- aligned=true (snapped_n>0) but the OTHER three drifted a full second. Scanned offsets and moved to 50 (5/6 snapped, 44ms). Lesson, now a rule: aligned=true is NOT alignment -- after beat_align, verify the screening metric (median of ALL cuts to onsets), and for an ambient track scan offsets for one where >=5/6 snap.
  4. deliverables substance FAIL (only 1 backed scene): mono_block copy used periods ("no BLAS. no Python.") and deliverables_check.norm KEEPS periods, so it did not substring-match the documented "no BLAS no Python at runtime"; norm STRIPS commas, so de-periodizing to "no BLAS, no Python" made the on-screen words literally trace to the study. Lesson: match on-screen product copy to the study's exact tokens minus commas, never with sentence periods.
- NEXT: the shader field on a near-black WARM palette only reaches ~50 mean (not the ~111 the 07-05 shader test hit on its palette), so 5/7 scenes were carried by bright TEXT on a dark field. It PASSED brightness (mean 80, the plate scenes lift it) but leans dark. The shader currently IGNORES design.background.intensity for brightness -- wire an exposure/brightness uniform to intensity so a dark-canvas run can lift its FIELD without a per-run shader edit. Also: the no-flash reference is ONE midpoint frame, fragile to which scene lands at t/2 -- reference a median of several mid-video frames so a legit bright plate-scene at the midpoint does not force a reorder.

## 2026-07-12 — mesh-llm by @michaelneale (the "camera THROUGH the mesh, and make the frame own its light" run)
- KEPT: the v11 producer phase KILLED the node-diagram+terminal default (the obvious treatment for a distributed-inference project) and committed to a continuous_camera ONER as the money shot -- one unbroken vertical glide through "your prompt / many machines / one answer" that makes distribution a JOURNEY you travel through, not an architecture slide. A device the last 12 runs never used. Color gate cleared cleanly with mesh-llm's own electric blue (#3b82f6, hue 217): a dark<->light value flip vs 07-10's light teal AND a hue+dE flip vs 07-11's orange. The brand_extract read the site's --primary (a neutral shadcn gray) as the accent; mining site.generated.css by hand found the REAL brand accent (--blue, 75 uses + a glow) -- always check the stylesheet for the saturated brand color when the extractor returns a gray "primary".
- KILLED (each caught by a gate or the filmstrip):
  1. brightness FAIL (mean luma 42, 29% bright) on a near-black canvas with no shader (07-11 used it, off-table). Root fix at the FRAME, not the grade: lifted canvas from the true brand near-black #09090b to the site's own dark surface #14171d (luma ~23, still dark/native) -- the background dominates the pixel count, so +18 luma on it moved the whole-frame mean 42 -> 61 and cleared the gate via the mean path. Lesson: on a dark palette with no shader, lift the CANVAS to a real (native) surface tone before fighting per-scene brightness.
  2. flash readability FAIL (p99.3=136, spread=21). The flash field was var(--accent) = #3b82f6 (luma 122), sitting JUST under the readability gate's dark/light boundary (median<128), so a bright inverted field got judged as a dark scene needing bright ink -- the exact "accent on the luma boundary" class from the 07-03/07-04 entries. Fixed in build_html: `.scene[data-tpl="flash"]` now blends the accent 60% with white, so the inverted field is UNAMBIGUOUSLY bright (median ~172) regardless of accent luma. Universal fix for any mid-luma accent.
  3. oner readability (the money shot): the centered glide line at 8.5cqw with a long middle block ("peers hold the layers") covered <0.7% of pixels so the top percentile read the field behind it (the coverage lesson again). Fixed by bumping .on-block to 12cqw + a text-stroke AND shortening the blocks to <=13 chars so the midpoint block is big and bright. The oner readability gate turned out NOT to be the failing scene at all on the first read -- I misattributed the flash's failure to the oner because both are index-ambiguous; ALWAYS extract the actual named scene's midpoint frame and measure before fixing.
  4. frame-pacing 360ms freeze inside the mono_block: static_breathe went still at the tail. Swapped to push_in (continuous zoom, never stops) -- a camera with real through-motion, not just an entrance.
  5. poster blank (std 4.5) twice, from trim jitter: detect_content_start returned i+1, but on a gradual cold-open fade that frame is still near-blank. Bumped it to i+2 in finish.py (deterministic across capture jitter). AND the open was a two_line with no project name on frame 0 -- the held-subject ticker only appears AFTER the first scene ends. Two fixes: (a) build_html now shows the ticker from t=0 when the open is NOT a title/logo_reveal (poster always carries the name); (b) switched the open to a `title` (eyebrow "mesh llm" + headline "a model too big") so the poster names the project outright -- the routine's recommended poster pattern, and it satisfies the frame-0 invariant unambiguously.
- NEXT: the flash-field white-blend (fix #2) should probably be conditional on the accent's luma -- for an already-bright accent it may wash the field too pale. Consider blending only when the accent's median luma is within ~20 of the 128 boundary. Also: a manual `--trim` to finish is unreliable because the input-seek (`-ss` before `-i`) snaps to a keyframe; when a poster needs a nudge, fix detect_content_start's margin, not the manual trim.
